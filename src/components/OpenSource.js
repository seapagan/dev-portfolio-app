import React from "react";

import styles from "../css/OpenSource.module.css";

const OpenSource = () => {
  return <section className={styles.section}>OpenSource</section>;
};

export default OpenSource;

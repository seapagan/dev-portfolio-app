import React from "react";

import styles from "../css/Hero.module.css";

const Hero = props => {
  return (
    <section id="hero" className={styles.hero}>
      HERO!!!!
    </section>
  );
};

export default Hero;
